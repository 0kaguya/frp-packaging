# vim:ft=systemd
[Unit]
Description="Start FRP Server for reverse proxy."
Requires=network.target
After=network.target

[Service]
ExecStart=frps -c /etc/frp/frps.toml

[Install]
WantedBy=multi-user.target
